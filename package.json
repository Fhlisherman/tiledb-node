{
  "name": "tiledb-node-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm --filter '*' run build",
    "clean": "rm -rf node_modules packages/*/node_modules packages/*/build packages/*/dist packages/*/tiledb-dist"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
